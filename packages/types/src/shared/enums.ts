export enum Role {
  ADMIN,
  USER,
}

export enum ShipmentStatus {
  PENDING,
  COMPLETED,
  CANCELLED,
}

export enum ProjectStatus {
  ACTIVE,
  COMPLETED,
  ARCHIVED,
}

export enum DocumentType {
  SPECIFICATION,
  DRAWING,
  CONTRACT,
  OTHER,
}

export enum CostType {
  MATERIAL,
  LABOR,
}
